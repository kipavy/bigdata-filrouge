apiVersion: 1

providers:
  - name: 'Velib Dashboards'
    orgId: 1
    folder: 'Velib'
    folderUid: 'velib'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
